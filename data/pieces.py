class Pieces:
    def __init__(self, piece, coord_piece):

        self.king():
            def king_movement()
                movement = abs(1)
                castling = (2,0)(1,0)
                long_castling_flag = True
                short_castling_flag = True
            def castling ():
                if move == (2,0):
                    board(7,7) = '--'
                    board(6,7) = self.king()
                    board(5,7) = self.rook()
                    rook = (king.coord[0]-1)
                else:
                    board(0,7) = '--'
                    board(2,7) = self.king()


        self.pawn_moves():
            if piece[0] = "b":
                if coord[1] != 1,7:
                    moves_list (0,-1)(0,-2)(1,-1)(-1,-1)
                elif coord[1] == 1:
                    moves_list (0, 1)(1, 1)(-1, 1)
                elif coord[1] == 0:
                    promotion():
            else:
                if coord[1] != 6, 0:
                    moves_list (0, 1)(0, 2)(1, 1)(-1, 1)
                elif coord[1] == 6:
                    (0, 1)(1, 1)(-1, 1)
                elif coord[1] == 0:
                    promotion():
            move_piece()
        # moves_allowed ()
        self.knight_moves():
            if abs(move)==3 
                pass
            else:
                pass
            # moves_list (1,2) (-1,2) (1,-2)(-1,-2) (-2, 1)(-2,-1) (2, 1) (2, -1)
        self.bishop()# NIEDOROBIONE
            if sum(coord) % 2 != 0 :
                if move [row] == move [col] or sum(move) == sum(coord)
                return moves_allowed
            else:
                if move [row] == move [col] or sum(move) == sum(coord)
            
        self.rook ()
            if (abs(0,move[1])) or (abs(move[0],0)):
                pass 
        self.queen()
            self.rook()
            self.bishop()
        self.king()
            self.queen
        pass

